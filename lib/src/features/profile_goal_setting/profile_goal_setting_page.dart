import 'package:flutter/material.dart';

class ProfileGoalSettingPage extends StatelessWidget {
  const ProfileGoalSettingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(),
      ],
    );
  }
}
