import 'package:flutter/material.dart';

class StudentProfileSettingPage extends StatelessWidget {
  const StudentProfileSettingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
